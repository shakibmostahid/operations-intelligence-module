services:
  app:
    volumes:
      - ../codes:/var/www/html
    command: ["dev"]
    ports:
      - "5173:5173"

  nginx:
    volumes:
      - ../codes/public:/var/www/html/public
      - ./configs/nginx.conf:/etc/nginx/conf.d/default.conf:ro
